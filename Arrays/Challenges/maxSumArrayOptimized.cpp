#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    int cumSumArray[n + 1];
    cumSumArray[0] = 0;
    int for (int i = 0; i < n; i++)
    {
    }

    return 0;
}
